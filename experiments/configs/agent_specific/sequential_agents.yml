# Sequential agent mode configuration

agents:
  planner:
    lm_name: "${model_specific.lm_name}"
    inference_config: "${model_specific.inference_config}"
    entropy_config: "${entropy_configs.entropy_config}"
    generation_config: "${base_config.generation_config}"
    sys_message: "maep.prompts:PLANNER_SYS"
    user_message: "maep.prompts:PLANNER_USER"
  solver:
    lm_name: "${model_specific.lm_name}"
    inference_config: "${model_specific.inference_config}"
    entropy_config: "${entropy_configs.entropy_config}"
    generation_config: "${base_config.generation_config}"
    sys_message: "maep.prompts:SOLVER_SYS"
    user_message: "maep.prompts:SOLVER_USER"
  critic:
    lm_name: "${model_specific.lm_name}"
    inference_config: "${model_specific.inference_config}"
    entropy_config: "${entropy_configs.entropy_config}"
    generation_config: "${base_config.generation_config}"
    sys_message: "maep.prompts:CRITIC_SYS"
    user_message: "maep.prompts:CRITIC_USER"
  judger:
    lm_name: "${model_specific.lm_name}"
    inference_config: "${model_specific.inference_config}"
    entropy_config: "${entropy_configs.entropy_config}"
    generation_config: "${base_config.generation_config}"
    sys_message: "maep.prompts:JUDGER_SYS"
    user_message: "maep.prompts:JUDGER_USER"