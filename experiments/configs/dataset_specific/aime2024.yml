# Dataset-specific configuration for AIME2024

data:
  data_name: AIME2024
  data_path: experiments/data/AIME2024
  split: train
  # The number of samples to be tested, -1 for all (30 test samples)
  data_num: -1
  # The number of samples for one inference
  batch_size: 1

task_type: "math"

# Generation configuration (overrides base_config.yml)
# AIME2024 problems require longer reasoning chains
generation_config:
  max_new_tokens: 8192