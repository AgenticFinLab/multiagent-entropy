# Dataset-specific configuration for Math500

data:
  data_name: Math500
  data_path: experiments/data/Math500
  split: test
  # The number of samples to be tested, -1 for all
  data_num: -1
  # The number of samples for one inference
  batch_size: 1

task_type: "math"

# Generation configuration (overrides base_config.yml)
# Math500 problems require longer reasoning chains
generation_config:
  max_new_tokens: 8192